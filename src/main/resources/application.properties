# Puerto del servidor (Render usa la variable PORT)
server.port=${PORT:8080}

# Configuración de la Base de Datos (Supabase con Variables de Entorno)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-eu-west-2.pooler.supabase.com:5432/postgres?prepareThreshold=0}
spring.datasource.username=${DB_USERNAME:postgres.upyktpxabwmcjtwqfbng}
spring.datasource.password=${DB_PASSWORD:Abecedario2552+}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Puerto de la aplicación
server.port=8080
